const Info = () => {
  return (
    <header className="header">
      <p className="header__label">My Weather App</p>
      <h1 className="header__title">Weather</h1>
      <p className="header__desc">
        Enter a city name and see the current weather requested from OpenWeather
      </p>
    </header>
  );
};

export default Info;
